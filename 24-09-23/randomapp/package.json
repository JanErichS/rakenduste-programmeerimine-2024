{
  "name": "randomapp",
  "version": "1.0.0",
  "description": "randomapp",
  "main": "index.js",
  "scripts": {
    "frontend": "cd frontend && npm run start",
    "backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express-validator": "^7.2.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
